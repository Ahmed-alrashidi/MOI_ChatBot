# --- Core AI & Hardware Acceleration ---
# PyTorch with CUDA support (A100 requires recent versions)
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0
accelerate>=0.27.0

# Critical for A100 Speedup (Flash Attention 2)
# Note: Installation might take time as it compiles CUDA kernels
# flash-attn>=2.5.0

# Vector Database (GPU Accelerated)
faiss-gpu>=1.7.2

# --- LLMs & NLP ---
transformers>=4.38.0
sentencepiece>=0.2.0
sentence-transformers>=2.5.0
tiktoken>=0.6.0
huggingface_hub>=0.20.0
langdetect>=1.0.9

# --- Text-to-Speech ---
gTTS>=2.5.0

# --- RAG & Orchestration ---
langchain>=0.3.0
langchain-community>=0.3.0
langchain-huggingface>=0.0.1
langchain-text-splitters>=0.0.1
langchain-core>=0.3.0

# --- Data Processing & Metrics ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0  # Required for Cosine Similarity in tests
tqdm>=4.66.0         # Required for Progress Bars
python-dotenv>=1.0.0

# --- User Interface ---
# STRICTLY PINNED: Gradio 4.x breaks the code syntax. Keep 3.50.2.
gradio==3.50.2